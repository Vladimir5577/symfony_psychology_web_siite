{% if home_themes and home_themes.posts|length > 0 %}
<div class="container marketing py-5">
    <h2 class="pb-2 border-bottom text-center text-body-emphasis mb-4">С чем работаю</h2>
    <div class="row g-4 row-cols-1 row-cols-md-2 row-cols-lg-4">
        {% for post in home_themes.posts %}
        <div class="col">
            <div class="card h-100 border-0 shadow-sm">
                <div class="card-body">
                    <h3 class="card-title h5 text-body-emphasis">{{ post.title }}</h3>
                    <p class="card-text text-body-secondary small mb-0">{{ post.description|default('') }}</p>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</div>
<hr class="featurette-divider" />
{% endif %}
