{# Contact page hero — intro block (data from Block contact_hero_block) #}
{% if contact_hero_block and contact_hero_block.posts|length > 0 %}
{% set post = contact_hero_block.posts|first %}
<div class="px-4 py-5 my-5 text-center">
    <h1 class="display-5 fw-bold text-body-emphasis">{{ post.title }}</h1>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4">{{ post.description|default('') }}</p>
    </div>
</div>
{% endif %}
